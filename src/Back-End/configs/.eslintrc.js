module.exports = {
    env: {
      node: true, // Configura o ambiente para Node.js
      es6: true, // Ativa as funcionalidades do ECMAScript 6+
    },
    extends: ['eslint:recommended'], // Usa as regras recomendadas pelo ESLint
    parserOptions: {
      ecmaVersion: 2018, // Usa o ECMAScript 2018
    },
    rules: {
      // Aqui você pode adicionar ou modificar regras do ESLint conforme necessário
    },
  };
  